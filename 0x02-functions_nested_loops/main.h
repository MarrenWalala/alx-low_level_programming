#include <unistd.h>
/**
 * _putchar - writes the character c the standard output
 *
 * Return: Always 0 (Success)
 */
int _putchar(char c);
void print_alphabet(void);
int add(int, int);
int print_sign(int n);
