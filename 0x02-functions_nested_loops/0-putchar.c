#include <unistd.h>
#include "main.h"
/**
 * main -Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char letter[8] = '_putchar';
	int a;

	for (letter = 0; letter <= 8; letter++)
		_putchar(letter[i]);
	_putchar('\n');

	return (0);
}
